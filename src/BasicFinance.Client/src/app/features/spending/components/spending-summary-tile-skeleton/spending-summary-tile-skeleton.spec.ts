import { SpendingSummaryTileSkeleton } from './spending-summary-tile-skeleton';

describe('SpendingSummaryTileSkeleton', () => {
  it('should create', () => {
    const component = new SpendingSummaryTileSkeleton();
    expect(component).toBeTruthy();
  });
});
