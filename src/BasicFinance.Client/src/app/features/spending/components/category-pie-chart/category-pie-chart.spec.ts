import { CategoryPieChart } from './category-pie-chart';

describe('CategoryPieChart', () => {
  it('should create', () => {
    const component = new CategoryPieChart();
    expect(component).toBeTruthy();
  });
});
