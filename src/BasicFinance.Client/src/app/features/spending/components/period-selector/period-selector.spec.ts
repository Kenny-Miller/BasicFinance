import { PeriodSelector } from './period-selector';

describe('PeriodSelector', () => {
  it('should create', () => {
    const component = new PeriodSelector();
    expect(component).toBeTruthy();
  });
});
