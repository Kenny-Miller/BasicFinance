import { CategoryBreakdownListSkeleton } from './category-breakdown-list-skeleton';

describe('CategoryBreakdownListSkeleton', () => {
  it('should create', () => {
    const component = new CategoryBreakdownListSkeleton();
    expect(component).toBeTruthy();
  });
});
