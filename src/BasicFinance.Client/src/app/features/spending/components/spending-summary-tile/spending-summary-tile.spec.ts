import { SpendingSummaryTile } from './spending-summary-tile';

describe('SpendingSummaryTile', () => {
  it('should create', () => {
    const component = new SpendingSummaryTile();
    expect(component).toBeTruthy();
  });
});
